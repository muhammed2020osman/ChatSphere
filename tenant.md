ممتاز ✅
إليك **البرومت الاحترافي الكامل (بدون أكواد)** يمكنك نسخه وإرساله لأي أداة ذكاء اصطناعي (مثل ChatGPT, Claude, أو Cursor) ليُنشئ لك **نظام Express + Next.js متعدد الشركات (Multi-Tenant SaaS)** من الصفر.

---

## 🚀 البرومت الكامل (بدون أكواد)

> 🧠 **العنوان:** بناء نظام Multi-Tenant متكامل باستخدام Express (Backend) وNext.js (Frontend)

---

### 🎯 الهدف:

تحويل المشروع إلى **نظام SaaS متعدد الشركات** بحيث:

* كل شركة (Tenant) تملك بياناتها المستقلة (مستخدمين، قنوات، رسائل، إعدادات).
* يمكن الوصول إلى كل شركة عبر  **company_id**.
* الواجهة الأمامية (Next.js) تتفاعل مع الـ API (Express) عبر JWT Tokens.
* دعم تسجيل شركة جديدة، تسجيل الدخول، إنشاء القنوات، وإرسال الرسائل.
* دعم فصل البيانات بشكل كامل على مستوى `company_id`.

---

### 🧱 المطلوب من الأداة:

أن تُنشئ مشروعين متكاملين:

#### 🅰️ المشروع الأول — **Backend (Express.js)**

* إنشاء REST API جاهز باستخدام Express + Knex أو Prisma.
* قاعدة بيانات واحدة تحتوي الجداول:

  * `companies`: بيانات الشركات (name, domain, plan_type, created_at)
  * `users`: المستخدمين مع `company_id`, `role`
  * `channels`: قنوات الشركة مع `company_id`
  * `messages`: الرسائل مع `channel_id` و`company_id`
* إضافة middleware باسم **tenantResolver** يحدد الشركة من:

  * subdomain (مثلاً `acme.example.com`)
  * أو header (`x-company-id`)
* إضافة middleware **authenticate** للتحقق من JWT token وربط المستخدم بالشركة.
* كل استعلام في النظام يجب أن يكون محميًا بـ `company_id` لمنع تسرب البيانات.
* إنشاء المسارات التالية:

  1. `/api/companies` → إنشاء شركة جديدة + مستخدم مسؤول
  2. `/api/auth/login` → تسجيل دخول الشركة
  3. `/api/channels` → إدارة القنوات الخاصة بالشركة
  4. `/api/messages` → إدارة الرسائل الخاصة بالشركة
* عند تسجيل شركة جديدة يتم إنشاء الشركة والمستخدم الأول مع JWT token مباشر.
* كل الردود تحتوي فقط بيانات الشركة الحالية.

---

#### 🅱️ المشروع الثاني — **Frontend (Next.js 15+)**

* استخدام **App Router** مع TypeScript.
* تصميم واجهة أنيقة RTL عربية لعرض:

  * صفحة تسجيل الدخول (مع إدخال company_id أو subdomain).
  * لوحة تحكم تعرض قنوات الشركة (Dashboard).
  * صفحة عرض الرسائل داخل القناة.
* استخدام Axios + Cookies لتخزين JWT وcompany_id.
* كل طلب إلى API يجب أن يرسل:

  * Header: `Authorization: Bearer <token>`
  * Header: `x-company-id: <id>`
* إنشاء صفحة إعداد الشركة (اسم الشركة + الخطة + المستخدمين).
* ربط النظام الأمامي مع الـ Backend بحيث يعمل مباشرة عند التشغيل المحلي.
* دعم اللغة العربية في كل النصوص (RTL).

---

### 🔐 الشروط العامة

* التوثيق عبر JWT فقط (بدون جلسات).
* عزل البيانات عبر company_id في كل الجداول.
* إمكانية استخدام subdomains مثل:

  * `acme.localhost:3000` → شركة Acme
  * `beta.localhost:3000` → شركة Beta
* الالتزام بالمعمارية القابلة للتوسّع (SaaS-ready).
* جميع الأكواد منظمة + تعليقات واضحة بالعربية.

---

### 📦 الناتج المطلوب

* مجلد `backend/` جاهز للتشغيل عبر `npm run start`.
* مجلد `frontend/` جاهز للتشغيل عبر `npm run dev`.
* جاهز للنشر على:

  * **Render أو Railway** للـ Backend.
  * **Vercel** للـ Frontend.
* يتضمن إعدادات `.env.example` وملفات `docker-compose.yml` اختيارية للتشغيل الكامل محليًا.

---

### ✅ المطلوب النهائي

> أن تُنشئ لي مشروعًا كاملًا Express + Next.js يدعم Multi-Tenant SaaS، كل شركة لها مستخدمين وبياناتها المستقلة، ويعمل محليًا وجاهز للنشر، مع فصل واجهة العميل عن الخادم.

---

هل ترغب أن أجعل هذا البرومت موجّه خصيصًا لأداة معينة (مثلاً **Cursor** أو **Claude** أو **ChatGPT Code Interpreter**) بصيغة مثالية لتلك المنصة؟
